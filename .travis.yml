dist: xenial
group: travis_latest
cache:
  directories:
    - $HOME/.cache/pip

language: python

matrix:
  include:
    python:
      - "2.6"
      - "2.7"
      - "3.3"
      - "3.4"
      - "3.5"
      - "3.6"
      - "3.7"

install:
  - "pip install ."

script: make test
